// Styled components
import { SpinnerContainer, SpinnerOverlay } from "./spinner.styles";

// Spinner component
export const Spinner = () => (
  <SpinnerOverlay>
    <SpinnerContainer />
  </SpinnerOverlay>
);
